#include "main.h"
#include <string.h>
/**
 * _strlen - function that prints the length of a string
 *
 * @s: parameter
 *
 * Return: 0 always
 */

int _strlen(char *s)
{
	int i;

	i = strlen(s);
	return (i);
}
