#include <stdio.h>
/**
 * main - a program that prints numbers with base ten
 *
 * Return: 0 (success)
 */
int main(void)
{
	int i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
